# v0.2.1 (2026-06-29)

### 강좌 학습 현황

- **Fixed**
  - 수강생 학습 현황 페이지네이션(#162) 적용 후 `getCourseLearningProgress` 반환 타입이 `StudentLearningProgressPage` 로 바뀌었으나 `CourseDetailClient` 가 배열로 받아 발생하던 배포 빌드 타입 오류 수정 (`page.content` 사용)

---

# v0.2.0 (2026-06-29)

### AI 채팅 스트리밍 (#N)

- **Added**
  - 일반 채팅 및 문제 풀이 채팅에 SSE 기반 스트리밍 응답 처리 추가
  - 스트림 이벤트(`room`, `done`, `error`, token)를 처리하는 공통 `streamChat` 유틸 추가

- **Changed**
  - 메시지 전송 후 응답 전체를 기다리던 방식에서 토큰 단위로 답변을 표시하도록 변경
  - 신규 채팅방 생성 시 스트림에서 전달된 roomId 기준으로 채팅방 이동/상태 갱신 처리

- **Fixed**
  - 스트림 중단 또는 네트워크 오류 시 기존 오류 모달과 에러 메시지로 실패 상태를 안내하도록 처리
  - 빈 assistant 메시지가 채팅 목록에 노출되지 않도록 렌더링 처리 보완

---

## 구글 소셜 로그인 (#167)

- **Added**
  - 구글 OAuth2 로그인 연동 — 동의 URL 이동 / 임시정보 조회 / 가입 완료
  - 신규 가입 추가정보 페이지 (닉네임 중복확인 + 전화번호 검증)

- **Fixed**
  - 기존 회원 구글 재로그인 시 헤더에 닉네임이 안 뜨던 문제 (프로필 1회 조회로 복구)

---

## 마이페이지 뱃지 (#157)

- **Added**
  - 사이드바 프로필 클릭 → 보유 뱃지 중 대표 뱃지 선택/장착
  - 장착한 뱃지가 사이드바·헤더 닉네임 옆에 실시간 반영

---

### 관리자 뱃지 관리 (#155, #159)

- **Added**
  - 뱃지 관리 페이지 — 뱃지 조회/등록
  - 뱃지 수정/삭제 기능

---

### 수강생 학습 현황 (#162)

- **Added**
  - 학습 현황 모달 서버사이드 페이지네이션
  - 수강생별 문제 풀이 현황(정답/오답/미제출) 조회

---

### 강좌·영상 강의 개선 (#N)

- **Changed**
  - 영상 강의 완료 기준을 끝까지 시청으로 강화 (재생 중 89% 상한, 종료 시 100%)
  - 첫 시청 중 배속 재생 차단 + 안내 문구 갱신

- **Fixed**
  - 강좌 등록/수정 버튼 연타 시 강좌가 여러 개 생성되던 문제 (동기 ref 가드)
  - 영상 완료 모달이 늦게/안 뜨던 문제 (재생바 끝 도달 즉시 트리거)

### 관리자 뱃지 관리

- **Added**
  - 관리자 뱃지 목록 조회 페이지 추가
  - 관리자 뱃지 등록 페이지 추가
  - 관리자 뱃지 수정 및 삭제 페이지 추가
  - 뱃지 이미지 업로드 및 미리보기 기능 추가
  - 뱃지 상태(`ACTIVE`, `INACTIVE`) 수정 기능 추가
  - 뱃지 등록·수정·삭제 확인 모달 추가
  - `/api/v1/admin/badges` 조회 및 등록 API 연동
  - `/api/v1/admin/badges/{badgeId}` 상세 조회, 수정, 삭제 API 연동

- **Changed**
  - 뱃지 목록을 카드형 UI로 구성하여 이미지, 이름, 설명, 필요 포인트, 상태, 등록일을 표시
  - 뱃지 등록·수정 화면을 강좌 등록 페이지와 유사한 폼 UI로 구성
  - 뱃지 목록 카드 클릭 시 수정 페이지로 이동하도록 변경
  - `/admin/badges/new`, `/admin/badges/[id]/edit` 진입 시 사이드바 뱃지 관리 메뉴 활성화

- **Fixed**
  - 뱃지 상세 조회 실패 또는 정보 없음 상태에서 안내 모달이 표시되지 않을 수 있는 문제 수정
  - 뱃지 수정 화면 모달 상태에서 성공 여부와 목록 이동 여부가 섞이던 상태값 분리

---

### 문제 풀이 페이지

- **Fixed**
  - 배포 환경에서 서버 컴포넌트의 상대경로 fetch로 `/api/v1/problem-categories` 호출이 실패하던 문제 수정
  - 문제 도메인 API 요청 시 서버 런타임에서는 `API_PROXY_TARGET` 또는 `NEXT_PUBLIC_API_URL` 기반 절대 URL을 사용하도록 보완
  - 서버 API base URL 누락 시 URL 파싱 오류 대신 설정 누락 오류가 명확히 발생하도록 수정

---

# v0.1.1 (2026-06-28)

### 기능

- **Added**
  - 없음

- **Changed**
  - 없음

- **Fixed**
  - 어드민 operations API `NEXT_PUBLIC_API_URL` 빈값 시 500 오류 / 흰 화면 핫픽스 (#152)

---

# v0.1.0 (2026-06-26)

### 기능

- **Added**
  - 로그인 추가 인증(step-up) — 미신뢰 기기 로그인 시 이메일 OTP 인증 + "이 기기 신뢰" 옵션 (#132)
  - 이메일(아이디) 찾기 — 이름 + 전화번호로 마스킹된 이메일 조회 (#140)
  - 강의자료 첨부 — 다중 파일 업로드 / 목록 / 다운로드 (#146)
  - 강좌 접근 가드 — 미수강 강좌 강의·문제 URL 직접 접근 차단, 잠금 순서 위반 시 이동 (#121)
  - 마이페이지 비밀번호 변경 — 비밀번호 확인 없이 바로 변경 폼 진입 (#117)
  - 최고관리자(master admin) 페이지 생성 (#115, #119)
  - 관리자 회원관리 페이지네이션 적용 (#104)
  - 문제풀이 페이지 문제 추천 모달 (#126)
  - 문제풀이 페이지 추천 강좌 기능 (#138)
  - 문제풀이 페이지 스켈레톤 UI (#130)
  - 문제풀이 페이지 사용자 편의 및 UI 개선 (#124)
  - 에러페이지 차르독 이미지 추가 (#144)
  - 마스코트 이미지 추가 (#142)

- **Changed**
  - 마이페이지 비밀번호 변경 UX 개편 — 이메일 OTP 제거, `PUT /me/password` 직접 연동
  - 로그인 API 프록시 설정 + 서버 컴포넌트 fetch 절대경로 수정 (#128)
  - `NEXT_PUBLIC_API_URL` 빈값 시 throw 제거 → `?? ""` 폴백으로 전환 (#136)

- **Fixed**
  - 강의 진도/완료 처리 — 문제 강의 구분, 완료 모달, 문제세트 경로 정정 (#134)
  - Docker 빌드 prerender 실패 — `useSearchParams` Suspense 래핑 (#111)

---
