// CSR - 알람 상세조회: 메모 저장, 상태 변경, 삭제처럼 즉시 반영되는 관리자 작업이 필요함
import AlramDetailClient from "@/features/admin/operations/components/AlramDetailClient";

export default function AlramDetailPage() {
  return <AlramDetailClient />;
}
