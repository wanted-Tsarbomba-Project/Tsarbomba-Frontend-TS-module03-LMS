// CSR - 관리자 회원 전체조회: 회원 목록을 세션 기준으로 조회하고 행 클릭으로 상세 화면에 진입함
import UsersClient from "@/features/admin/operations/components/UsersClient";

export default function Page() {
  return <UsersClient />;
}
